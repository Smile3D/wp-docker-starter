# Скопируй в .env.deploy и заполни реальными данными хостинга.
# Этот файл НЕ должен попадать в git — добавь .env.deploy в .gitignore

REMOTE_USER=your_ssh_user
REMOTE_HOST=your-host.com
REMOTE_PATH=/home/your_ssh_user/public_html
REMOTE_URL=https://mysite.com
